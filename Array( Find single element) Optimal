#include<vector>

int getSingleElement(vector<int> &arr){
	int n=arr.size();
	int i=0;
	while(i<n){
          if (arr[i] == arr[i + 1]) {
            i += 2;
            continue;
          }
         if (arr[i] != arr[i - 1]){
			return arr[i];
        }
      }
		
}
