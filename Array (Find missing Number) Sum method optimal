#include<map>
#include<vector>
int missingNumber(vector<int>&a, int N) {
    int Tsum=(N*(N+1))/2;
    int arrSum=0;
    for(int i=0;i<a.size();i++){
        arrSum+=a[i];
    }
    return Tsum-arrSum;
}
